export type ProductProps = {
    data: {
        id: number,
        title: string,
        seo_title: string,
        seo_Description: string,
        amount: number,
        originalPrice: number,
        offerPercentage: number,
        img: string,
    }
}
